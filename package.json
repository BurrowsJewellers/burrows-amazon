{
  "name": "burrows-amazon",
  "version": "0.1.0",
  "private": true,
  "description": "Lists the Burrows Jewellers catalogue on Amazon AU, and shows what didn't list and why",
  "main": "src/server.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node scripts/migrate.js",
    "match": "node scripts/match.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
